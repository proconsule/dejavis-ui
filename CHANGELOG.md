### [0.0.1] ###

- First public version

#### Known Issue ####

- On Win32 version Vulkan Decode is disabled and instead is used HW Api
- On MacOS vulkan deocde/encode are not present so it default to videotoolbox hw api
- Not a real issue but is a 0.0.1 version so i suggest to not use in production